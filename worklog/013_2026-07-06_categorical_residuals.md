# Worklog chunk 013 - 2026-07-06

## W1.4: the JW kernel decision, CA-74 (2026-07-06)

### Context

Continuation of the consistency-layer plan: W1.4 per
reviews/2026-07-06_pipeline_consistency_scoping/PLAN.md, interpreting the
CA-73 decision datum (322/322/323).  Tobias directed: orchestrate, delegate
liberally to opus and codex.  Fan-out: three parallel workers (Opus
derivation, GPT-5.5/codex xhigh numerics, Opus source anchors) +
orchestrator cross-verification; full record in
reviews/2026-07-06_ca74_jw_kernel_decision/ (ORCHESTRATION.md).

### What changed

- Shard CA-74-JW-KERNEL-DECISION (75 shards, 173 pp): parity-refined
  multiplicities in closed form m_1^± = (F_{2L-1} ± F_{L+1})/2,
  m_tau^± = (F_{2L} ∓ F_L)/2; parity-preserving dims P(L) = 9, 51, 322,
  2135, 14445 (L=2..6); Theorem 74.1: ker rho_4 = span{iota(p_4)} and
  rho_4 surjects onto the parity-preserving subalgebra — the ONLY numerical
  input is rank rho_4 = 322, everything else analytic and locally sourced
  from SRC-TL-JONES (Iohara-Lehrer-Zhang); Conjecture 74.2 (upgraded CA-69:
  kernel = two-sided ideal generated by corner JW embeddings) with
  unconditionally rigorous lower bounds 53/1066 on ker dim at L=5,6;
  Gram-rank deferral behind W1.5/T1 recorded.
- CONVENTIONS (r): SRC-TL-JONES import bridge appended (their loop
  -(q+q^{-1}), root order l=|q^2|; our beta=+phi is their l=5 point; loop
  sign is gauge, kappa_tau=+1).
- src/JonesWenzlKernel.jl (174 ln, written by codex): corner unit Pi_A,
  corner-compressed dense TL generators, Wenzl recursion carrying a formal
  word expansion alongside the matrices.  Testset "jones-wenzl kernel
  decision (CA-74)": 113 assertions, standalone green; three mutations RED
  (Wenzl coefficient 99/14, recursion beta 88/25, corner unit 105/8),
  reverted.

### KEY RESULT

The CA-69 conjecture is SETTLED at L=4: opnorm(rho(iota(p_4))) = 2.3e-15
(p_2, p_3 survive with norm 1), so the one-dimensional kernel is exactly
the corner-embedded first negligible Jones-Wenzl projector.  Upgrade found
by the derivation worker: the registered ILZ source gives
ker(TL_4(phi) -> End(tau^x4)) = span{E_4} outright (radical dim 1 at
n = l-1, tr(E_4) = 0, coefficient 1 on the identity), so the kernel
IDENTITY is analytical; surjectivity onto parity-even (rank 322) is the
single numerical fact, plus an independent positivity/quantum-trace
argument.  Parity mechanism: charge 1 is even-heavy by +F_{L+1}, charge
tau odd-heavy by -F_L; codex path counts and opus closed forms agree
independently through L=6.

### Frictions / dead ends / findings

- literature/md/1710.07362 truncates the Wenzl recursion's diagram factor
  — the testset therefore pins the DEFINING JW properties (idempotent,
  E_j-annihilation, unit coefficient 1), not the remembered formula.
  Acquisition gaps recorded in the shard boundary: explicit algebraic
  Wenzl recursion (Kauffman-Lins or Morrison 1503.00384) and the
  categorical negligible formalism (EGNO Ch. 8 / Goodman-Wenzl).
- ILZ extraction line :666 ("dim Q_8 = 233") contradicts the paper's own
  l=5 module formulas (610 = F_15); treated as an extraction artifact;
  only dim Q_4(5) = 13 is used, triple-checked.
- Anchors worker: Dtl_pgl_ell.06.tex contains NUL bytes (grep sees binary
  — pipe through tr -d '\000' or use Read); it also saw shell output
  corruption replacing technical words, so all quotes were re-taken with
  Read against raw files.
- Zero disagreements across the three workers; the orchestrator re-ran the
  standalone testset (113/113) and spot-verified every load-bearing ILZ
  anchor before writing the shard.

### Acceptance (per the platform gates)

- make check-report-shards: 75 shards green.  make report: 173 pp clean,
  CA-74 in the TOC.
- using CftAnyons loads; CA-74 testset standalone: 113/113, re-run
  independently by the orchestrator after codex finished; tree re-diffed
  at add time, no live workers.

### Pointers

- Shard CA-74; src/JonesWenzlKernel.jl; CONVENTIONS (r) import caveat;
  reviews/2026-07-06_ca74_jw_kernel_decision/ (ORCHESTRATION.md,
  derivation_opus.md, numerics_codex.md, sources_anchors.md,
  codex_prompt.md, codex_exec.log).
- PLAN status: W1.4 done.  Next: W1.5 (CA-75 endpoint-closing ledger),
  W1.6 (soft-null bridge).  Any L=5 kernel confirmation must be
  combinatorial (matrix rank at L=5 is budget-banned).

## W1.3: categorical residual set, CA-73 + the L=4 kernel datum (2026-07-06)

### Context

Continuation of the consistency-layer plan (chunks 012): W1.3 per
reviews/2026-07-06_pipeline_consistency_scoping/PLAN.md.  Mid-block Tobias
issued two platform directives: (i) we are on a laptop — compute-intensive
tasks need tight budgets and close monitoring; (ii) full Pkg.test() runs are
SUSPENDED for the foreseeable future ("all future learnings are unlikely to
critically depend on test suite passing").  Both are now recorded in
AGENTS.md (Build & test platform note) and agent memory; acceptance gates
for Julia work are now: the new testset green STANDALONE + a package-load
smoke check.  The shard guard and latexmk remain mandatory.

### What changed

- Shard CA-73-CATEGORICAL-RESIDUAL-SET (74 shards, 170 pp): the CA-38/39/42
  coboundary + Witt/Virasoro residual calculus ported to A_L =
  End_C(O^{tensor L}) (nothing in the qubit derivations used the Pauli
  basis — the port is exact); open-window mode identities with a sharp
  edge-remainder lemma; two-constant (c_L, c*_L) closure per CONVENTIONS
  (s); the scale-L residual family R_L(h) defined (seam S3's missing
  object); Lemma 73.1 (exact transport of the residual calculus through
  corner morphisms — residuals of h map to the same residuals of the
  STRETCHED densities theta(h_j), vacuum-pinned inserted sites); the
  stretched-vs-fine dichotomy (naive descent-compatibility FAILS — the
  precise residual-level sense in which bare vacuum insertion is naive);
  Prop 73.2: the stretched-augmented coherent family R^coh makes the CA-72
  descent contract hold by construction, at the price of stronger fine
  constraints; the dressed alternative recorded as the W2.1 equation.
  Periodic modes stay D1-gated.
- src/FibonacciLocalOperators.jl (265 ln): the 2-site local-operator
  machinery on the CA-66 path basis — the single nontrivial Fibonacci
  F-matrix anchored to Trebst (:320-323, eq 2.4), unit-label moves trivial
  per (b); Tier-1 generators (occupancies, hop, raw-cup pair with u^2 =
  phi u, dense e_j).
- src/CategoricalResiduals.jl (264 ln): ported residual densities,
  coboundary distances (pinv + independent QR), mode-identity edge defect,
  transport tests, and dilute_image_dimension / parity_even_dimension.
- Testset "categorical residual set (CA-73)": 262 assertions, standalone
  green; four mutations RED (F-matrix sign 238/24, hop amplitude 242/20,
  cup normalization 243/19, coboundary sign 260/2), reverted.

### KEY RESULT (W1.4 decision data, to be interpreted in CA-74)

Dimension of the algebra generated by the Tier-1 dilute generators inside
A_L, vs the parity-even subalgebra dimension, vs dim dTL_L = M_{2L}:

    L=2:  9 /  9 /  9      L=3:  51 /  51 /  51      L=4:  322 / 322 / 323

At L=4 the dilute image EQUALS the full parity-even subalgebra (322 =
9^2+4^2 + 9^2+12^2, from the parity-refined multiplicities 13 = 9+4,
21 = 9+12) and is one short of M_8 = 323: the CA-69-predicted Jones-Wenzl
kernel exists and is EXACTLY ONE-DIMENSIONAL, and rho_4 is surjective onto
the parity-even subalgebra.  Robust across rank tolerances 1e-6..1e-10 and
generator-set enlargement; independently re-run by the orchestrator
(identical numbers).  Upgrade candidate for CA-69's conjecture: rho_L
surjects onto the parity-even subalgebra for all L, with a growing JW-type
kernel from L=4.  The explicit kernel element (the dilute negligible
projector at beta = phi) is not yet identified — that is CA-74's target.

### Frictions / dead ends / findings

- The dense braid relation e_j e_{j+1} e_j = e_j holds ONLY on the dense
  corner (on the full dilute space e_1 e_2 e_1 = 0: u_j annihilates empty
  neighbours) — the computational shadow of CA-69's pi_A dTL pi_A = TL.
  The orchestrator's agent spec had stated it unrestricted; the agent
  caught and scoped it (second spec correction caught by an implementer
  this session).
- Pure-hopping density: conservation residual is a coboundary to 1e-14
  (hopping conserves momentum) — checked via two independent solvers.
- Generic-density coboundary distance is ill-conditioned (pinv vs QR agree
  only to 1e-4) — the CA-64 singular-KKT phenomenon recurs; asserted
  qualitatively, not pinned.
- Mid-task compute corrections worked: L=5 cancelled before any heavy
  numerics started; no full-suite runs; machine stayed responsive.

### Acceptance (per the new platform gates)

- make check-report-shards: 74 shards green.  make report: 170 pp clean.
- using CftAnyons loads; CA-73 testset standalone: 262/262 green;
  headline L=2,3,4 dimension data independently reproduced by the
  orchestrator before commit; tree re-diffed at add time, no live writers.

### Pointers

- Shard CA-73; src/FibonacciLocalOperators.jl, src/CategoricalResiduals.jl;
  AGENTS.md platform note; PLAN items W1.3 done, W1.4 data in hand.
- Next per plan: CA-74 (W1.4 interpretation: parity-even combinatorics
  derivation, the kernel identification target, Gram-rank shadow deferral),
  then W1.5 (endpoint-closing ledger), W1.6 (soft-null bridge).
