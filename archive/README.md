# archive/ — deep storage

These materials are kept for provenance but are **not canonical** and
should not be consulted by agents during normal work.

**Do not read files under this directory unless explicitly asked.**
Per `CLAUDE.md`: the canonical mathematical narrative lives in
`summary.tex`; the canonical definitions in `GLOSSARY.md`; everything
under `archive/` has been superseded.

## Subdirectories

- `chats/` — `chat1-4.md`, the four LLM-conversation transcripts that
  `summary.tex` was distilled from. Contain superseded definitions,
  notational inconsistencies, and false starts that `summary.tex` has
  already reconciled.
- `cad-meta-snapshot/` — heavy meta documents from
  `cft-anyons-deprecated/` (handoff, MASTER_PROVENANCE,
  NEXT_AGENT_HANDOFF, FORMALISATION_PLAN). Useful as historical record;
  not authoritative.
- `mma-archive-v0-snapshot/` — the v0 generation of
  `microscopic-mobile-anyons/archive/v0/`. Contains confirmed
  vacuum-indexing bugs (STL-1 through STL-4) documented in its own
  `reviews/ultracritical_audit_2026-01-03.md`. Reference only.
